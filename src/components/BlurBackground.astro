<div class="blur-wrapper absolute inset-0 -z-10 overflow-hidden">
  <div class="blur blur-1"></div>
  <div class="blur blur-2"></div>
  <div class="blur blur-3"></div>
</div>

<style>
  :root {
    --color: #07559f;
    --blur-1-size: clamp(400px, 60vw, 60vw);
    --blur-2-size: clamp(300px, 40vw, 40vw);
    --blur-3-size: clamp(100px, 10vw, 10vw);
    --blur-amount: clamp(40px, 5vw, 90px);
  }

  .blur {
    @apply absolute rounded-full;
    z-index: 1;
    filter: blur(var(--blur-amount));
    background: var(--color);
  }

  .blur-1 {
    height: var(--blur-1-size);
    width: calc(var(--blur-1-size) * 1.2);
    top: 10vh;
    left: 10vw;
  }

  .blur-2 {
    filter: blur(var(--blur-amount)) brightness(1.3);
    height: var(--blur-2-size);
    width: calc(var(--blur-2-size) * 0.75);
    top: 20vh;
    right: 20vh;
  }

  .blur-3 {
    filter: blur(var(--blur-amount));
    height: var(--blur-3-size);
    width: calc(var(--blur-3-size) * 2);
    bottom: 20vh;
    left: 20vw;
  }
</style>
